# Alternative GPU stack for older NVIDIA GPUs where the CUDA 12.8 PyTorch wheel
# raises "no kernel image is available for execution on the device".
# Use this instead of requirements-gpu.txt only if the CUDA arch diagnostic shows
# that the GPU compute capability is not in torch.cuda.get_arch_list().
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

torch==2.5.1+cu121
torchvision==0.20.1+cu121
